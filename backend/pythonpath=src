#!/usr/bin/env bash
set -euo pipefail

APP_ROOT=${APP_ROOT:-/app}
cd "$APP_ROOT"

# Ensure the backend runs with the expected module path even when invoked via the
# Railway start command `pythonpath=src`.
export PYTHONPATH=${PYTHONPATH:-"$APP_ROOT/src"}
exec "$APP_ROOT/start.sh" "$@"
