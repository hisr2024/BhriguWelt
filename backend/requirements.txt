# Swiss Ephemeris is optional; install `pyswisseph` manually for high-precision
# astronomy when network access to PyPI is available. The fallback logic keeps
# tests deterministic when the binary wheel cannot be fetched in restricted
# environments.
pytz>=2024.1
geopy>=2.4.1
timezonefinder>=6.4.1
scikit-learn>=1.3.0
pandas>=2.2.2
sentry-sdk>=1.45.0
aiohttp>=3.9.5
