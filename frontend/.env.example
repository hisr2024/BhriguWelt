# URL of your running backend (Render, Railway, localhost)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Default locale for UI copy
NEXT_PUBLIC_LOCALE=en
# Optional monitoring endpoint for the web bundle
NEXT_PUBLIC_SENTRY_DSN=
